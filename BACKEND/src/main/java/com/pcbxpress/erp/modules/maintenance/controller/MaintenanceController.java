package com.pcbxpress.erp.modules.maintenance.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * Main maintenance module controller
 * Provides entry point for maintenance-related operations
 */
@RestController
@RequestMapping("/api/maintenance")
public class MaintenanceController {
    
    // Main maintenance controller - delegates to submodule controllers
}